# nvim-config

My own neovim config from scratch.

## Requirements

- `neovim` >= 0.12
- `npm` for some mason packages
- some tools like `fzf` which I will remember when I install this on other
  machines.
